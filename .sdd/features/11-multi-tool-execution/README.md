# Multi-Tool Execution

修复 AI 调查执行循环"每轮只执行模型规划的第一个工具、其余被丢弃"的缺陷，让模型每轮规划的多个工具调用全部按序执行；移除失效的 `parallel_tool_calls=False` 与机械的收敛守卫，改用轮次预算 + 轮次提醒。

## 文档

- [design.md](design.md)：现状、目标、做法与已确认决策。

## 状态

设计已确认（用户决策）；实现计划待审批。
