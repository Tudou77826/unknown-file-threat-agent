# Common Scenario Rules

所有场景遵守以下规则：

- 未知文件不等于恶意文件。
- 上游进程链、告警标签和威胁情报首先是 Claim。
- 文件落盘、文件执行、后续行为和实际影响必须分开证明。
- 时间接近不能替代实体归属和因果关系。
- 相同端点、账号或 Hash 可以形成调查线索，不能自动证明传播或控制。
- 完整无结果与数据不可用必须通过 Coverage 区分。
- 每个恶意场景必须检查相应反证。

场景文档描述最低证据问题，不规定具体数据平台实现。
