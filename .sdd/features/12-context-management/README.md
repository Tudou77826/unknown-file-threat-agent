# Context Management

为 AI 调查 agent 补齐上下文管理机制：进度投影 + token 预算 + 分阶段压缩。解决 feature 11 暴露的"模型查全后仍反复探查"的失忆循环（根因：唯一上下文管理是一行 `traces[-12:]` 硬截断，无压缩/摘要/进度投影/token 预算）。

设计参考 `D:\dev\workspace-ai\chrys` 的上下文管理机制，但不照搬其 4 阶段压缩 + spill 磁盘归档 + 校准的完整框架——按本项目 demo 阶段的规模做最小正确版。

## 状态

设计已定稿（含窗口配置项 100K、结构化进度投影、token 预算截断、摘要降级）；实现计划待出。
